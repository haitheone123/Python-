import math
t = int(input())
def median_max(t):
    for i in range t:
        length = int(input())
        sum = int(input())
        middle = (length/2)+(length%2)
        x = sum/(length-(mid-1))
        return x
median_max((t))